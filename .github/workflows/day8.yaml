name: "Day 8 - Debugging Workflows"

on:
  workflow_dispatch:

jobs:
  broken-job:
    runs-on: ubuntu-latest
    steps:
      - name: This step will work
        run: echo "Step 1 is fine!"

      - name: This step will BREAK
        run: echo "This command is wrong!"

      - name: This step will never run
        run: echo "Will I run?"